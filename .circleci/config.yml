version: 2

jobs:
  build:
    machine: true
    steps:
      - checkout
      - run:
          name: build
          command: |
            make build-all
      - run:
          name: running test 
          command: |
            make test-all
